# Project properties 
project.desc=JiBX/WS XML binding web services
project.name=jibx-ws
project.version.target = 0.9.1

# Common properties
lib.dir=${basedir}/dep-lib
lib.spring.dir=${basedir}/spring-lib
target.dir=${basedir}/target
reports.dir=${target.dir}/reports
ivy.file=${build.dir}/ivy.xml

# Main Source
main.src.dir=${build.dir}/src
main.target.dir=${target.dir}/classes

# Test properties
test.src.dir=${build.dir}/test
test.target.dir=${target.dir}/test-classes
test.results.dir=${target.dir}/test-results

# Documentation properties
doc.target.dir=${target.dir}/docs
javadoc.user.dir=${doc.target.dir}/api
javadoc.dev.dir=${doc.target.dir}/api-dev
site.doc.src.dir=${basedir}/docs-src
site.doc.target.dir=${doc.target.dir}

# Quality report properties
reports.dir=${target.dir}/reports

# Distribution properties
package.dir=${basedir}/lib
package.file=${package.dir}/${project.name}.jar
dist.dir=${target.dir}/distrib
dist.lib.dir=${target.dir}/dep-lib
dist.lib.spring.dir=${target.dir}/spring-lib
dist.lib.minimal.dir=${target.dir}/tmp-minimal-dep-lib

# Publishing properties
org.apache.maven.ant.version=2.0.10
